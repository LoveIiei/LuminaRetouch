# LuminaRetouch Dependencies
# Professional Portrait Retouching Application

# ============ REQUIRED (Core functionality) ============

# UI Framework
PySide6>=6.5.0

# Image Processing
numpy>=1.24.0
opencv-python>=4.8.0

# Face Detection & Landmarks
mediapipe>=0.10.0

# ============ RECOMMENDED (AI Enhancement) ============
# For professional-grade skin retouching, install AI models:

# Step 1: Install PyTorch
# With CUDA (GPU acceleration - recommended):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# CPU only (slower but works everywhere):
#   pip install torch torchvision

# Step 2: Install GFPGAN for AI face enhancement
#   pip install gfpgan

# The model weights (~350MB) will auto-download on first use.
# GPU is recommended for real-time performance!

# ============ OPTIONAL (Advanced upscaling) ============
# For AI-powered image upscaling:
#   pip install basicsr realesrgan
#
# Note: If you get "functional_tensor" error after installing basicsr:
#   python fix_basicsr.py
